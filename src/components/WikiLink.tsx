/** Inline styles for the `.wikilink` / `.wikilink.broken` classes emitted by
 * the markdown-it wiki plugin. We use a stylesheet (rather than tailwind on
 * dangerouslySetInnerHTML) because the renderer controls class names.
 */
import './WikiLink.css'
export {}
